# Ruff config. Keep the default-ish rule set (pyflakes + common pycodestyle),
# and ignore E402 (module-level import not at top) because main.py loads .env
# before importing modules on purpose.
line-length = 88

[lint]
select = ["E4", "E7", "E9", "F"]
ignore = ["E402"]
