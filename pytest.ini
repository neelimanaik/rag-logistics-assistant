[pytest]
# Add the repo root to sys.path so tests can `import src...` without installing.
pythonpath = .
testpaths = tests
# Silence a known third-party deprecation from starlette's TestClient (not our code).
filterwarnings =
    ignore:Using `httpx` with `starlette.testclient` is deprecated
