[mypy]
# Third-party libraries (faiss, openai, langchain, jwt, ...) ship without type
# stubs; don't fail on their missing types. Our own code isn't fully annotated
# yet, so mypy runs as an advisory check rather than a strict gate.
ignore_missing_imports = True
